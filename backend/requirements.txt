# ── Core Framework ──────────────────────────────────────────
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-multipart>=0.0.20  # untuk upload file

# ── Database (async PostgreSQL via SQLAlchemy) ──────────────
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.30,<1.0
greenlet>=3.0  # diperlukan SQLAlchemy async

# ── Supabase Storage ────────────────────────────────────────
supabase>=2.0,<3.0

# ── HTTP Client (untuk panggil ML service) ──────────────────
httpx>=0.28,<1.0

# ── PDF Parsing ─────────────────────────────────────────────
pdfplumber>=0.11,<1.0

# ── NLP (ekstraksi skill) ───────────────────────────────────
nltk>=3.9,<4.0
Sastrawi>=1.0.1,<2.0  # stemmer Bahasa Indonesia

# ── ML / Matching ───────────────────────────────────────────
scikit-learn>=1.5,<2.0

# ── Testing ─────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
httpx  # juga digunakan untuk TestClient async
